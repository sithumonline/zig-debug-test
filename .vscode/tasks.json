{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "zig build"
        },
        {
            "label": "test-build",
            "type": "shell",
            "command": "mkdir -p zig-out/bin/ && zig test src/introducing_zig_test.zig -femit-bin=zig-out/bin/test-debug"
        }
    ]
}